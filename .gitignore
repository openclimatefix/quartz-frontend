node_modules
.turbo
build/
dist/
.next/
.env

# The forecast sonifier testbed: a spike for tuning the audio health check by ear.
# Vendored third-party code, generated audio samples and a fetched data library,
# none of it imported by any app. Kept on disk because the tuning is worth
# rerunning, deliberately never committed. The shipped version lives in
# apps/nowcasting-app.
apps/ux-demos/
