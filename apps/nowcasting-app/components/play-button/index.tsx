import React from "react";
import Ui from "./ui";

type PlatButtonProps = {};

const PlatButton: React.FC<PlatButtonProps> = () => {
  return <Ui onClick={() => {}}></Ui>;
};

export default PlatButton;
