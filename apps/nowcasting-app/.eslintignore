**/*.generated.*
node_modules/*
node_modules
.next/*
**/data/*
**/data/**/*

# Vendored third-party source, kept byte-identical to upstream so it can be
# diffed against a newer release. Prettier would reformat all of it.
lib/vendor/
