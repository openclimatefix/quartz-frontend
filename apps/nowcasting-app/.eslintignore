**/*.generated.*
node_modules/*
node_modules
.next/*
# Anchored to the app root: this is meant for the large GeoJSON/fixture dir at `data/`.
# Unanchored `**/data/*` also swallowed `hooks/data/*`, silently exempting the data hooks
# from linting.
/data/*
/data/**/*
