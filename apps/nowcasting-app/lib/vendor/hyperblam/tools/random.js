const random={oneOf:array=>(array=array||[0])[Math.floor(Math.random()*array.length)],chance:fraction=>Math.random()<=fraction,floatBetween:(min,max)=>(max=max<(min=min>max?max:min)?min:max,Math.random()*(max-min)+min),integerBetween:(min,max)=>(max=max<(min=min>max?max:min)?min:max,Math.floor(Math.random()*(max-min+1)+min)),shuffle(array){let currentIndex=array.length;for(;0!=currentIndex;){let randomIndex=Math.floor(Math.random()*currentIndex);currentIndex--,[array[currentIndex],array[randomIndex]]=[array[randomIndex],array[currentIndex]]}return array},some(array,amount){let shuffled=this.shuffle(array);return amount=amount||this.integerBetween(1,array.length),shuffled.slice(0,amount)}};export{random};